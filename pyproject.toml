[project]
name = "clara-cartesia-agent"
version = "0.1.0"
description = "Cartesia code-mode voice agent for Hotel Heidehof (Clara)."
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.13",
    "loguru>=0.7.0",
    "httpx>=0.27",
]

[tool.setuptools]
py-modules = ["main"]
